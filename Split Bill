def calculate_split_bill(total_bill, number_of_people, tip_percentage):
    tip = total_bill * (tip_percentage/100)
    bill = total_bill + tip
    return int(bill / number_of_people)

print(calculate_split_bill(300000, 4, 10))
